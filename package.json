{
  "name": "automaton",
  "version": "1.0.0",
  "description": "A TypeScript library for building automatons",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "author": "Malcolm Anderson",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "20.4.6",
    "gts": "^5.0.1",
    "typescript": "~5.1.6"
  }
}
