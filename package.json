{
  "name": "automaton-kit",
  "version": "1.0.0",
  "description": "A JavaScript library for building automatons",
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "test": "mocha --reporter spec",
    "lint": "eslint",
    "compile": "tsc",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "author": "Malcolm Anderson",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.3.6",
    "@types/mocha": "^10.0.1",
    "@types/node": "^20.4.6",
    "chai": "^4.3.8",
    "eslint": "^8.48.0",
    "mocha": "^10.2.0",
    "typescript": "~5.1.6"
  }
}
